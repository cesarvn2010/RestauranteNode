<!-- views/newPrato.ejs -->
<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <title><%= pagina.title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body class="container">
    <header>
        <% include ../partials/header %>
    </header>
    <main>
      <h1><%= pagina.title %></h1>
      <form action="<%= action %>" method="POST">
        <p>Nome do Prato: <input type="text" name="nomePrato" value="<%= doc.nomePrato %>"/></p>
        <p>Preço: <input type="number" name="preco" value="<%= doc.preco %>"/></p>
        <input type="submit" value="<%= pagina.value %>" />
      </form>
    </main>
    <footer>
        <% include ../partials/footer %>
    </footer>
  </body>
</html>